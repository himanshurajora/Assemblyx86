; For comment we just use a semicolon
; lets say if we want to comment an mov statement

; the comment would look something like below
mov ax, 10 ; Move the value ten to the ax, 16 bit accumulator storage
